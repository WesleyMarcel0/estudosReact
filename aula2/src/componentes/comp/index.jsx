export function Header(){ //vamos exportar a função para ligar a aplicação
    //criando um componente jsx
    //fica mais facil dividir cada componente
    return(
        <header>Header</header>
    )

// SEMPRE SALVE O COMPONENTE EM LETRA MAIUSCULA
// as funçoes nao faz mal mas se for um componente
//tambem da para fazer uma *função const nome = () => {}
// essa função é a arrow function
}